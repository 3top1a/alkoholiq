// A very simple program that takes in input and prints it out in reverse.
// An "unlimited" input size is achieved via the stack.

set zero 0
push zero

read in

while_nz in
    push in
    read in
end

pop out
while_nz out
    print out
    pop out
end
